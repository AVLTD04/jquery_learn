// JavaScript Program to Find the Square Root
// take the input from the user

const number = prompt('Enter the number: ');
const result = Math.sqrt(number);
var string = (`The square root of ${number} is ${result}`);